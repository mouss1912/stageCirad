
package javaapplication;

import java.util.ArrayList;
import java.util.List;

/**
 *
 * @author moussa
 */
public class Resultat {
    private List<List<String>> listePanier;
}
/*
 public void deleteElement(ArrayList ligneSupprimee){//remanier
        listePanier.remove(ligneSupprimee);
 */

/*/   int l = 0; // numéro de la ligne dans resultatDefinitif
      for (ArrayList<String> suprim : resultatsDefinitif) //Pour chaque String suprim dans resultatsDefinitif
      {     System.out.println("Je suis dans l'etape1"+elem);
          if( suprim.contains(elem) )
          {//hasmap ( valeurs) = la cellule
              System.out.println("Je suis dans l'etape2"+elem);
             resultatsDefinitif.remove(suprim);
                           
             // List<String> ligneSupprimee = new ArrayList<>();// Ajout des cellules récuperer dans une liste
             // System.out.println("---------liste de la cellule supprimee----------");
              
              {
                 // if( l < resultatsDefinitif.get(l).size())
                  {
              //        System.out.println("valeur ajouter à la l : " + resultatsDefinitif.get(l));
                      
               //       ligneSupprimee.remove(resultatsDefinitif.get(l).size());
                      
                  }
              }
          }
      }
*/